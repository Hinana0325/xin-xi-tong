[package]
name = "warrantos-spec"
version = "0.1.0"
edition = "2021"
description = "WARRANT OS — 类型即规格:八个命名空间的形式化定义"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
name = "warrantos_spec"
path = "src/lib.rs"